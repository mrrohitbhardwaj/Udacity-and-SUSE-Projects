apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-techtrends.fullname" . }}
  labels:
    {{- include "helm-techtrends.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-techtrends.selectorLabels" . | nindent 4 }}
